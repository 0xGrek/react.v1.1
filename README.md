# react.v1.1

Early React practice repository.

## Contents

- `first` - expenses list components
- `first.practic` - small props/component exercise

## Status

Legacy learning project. `react.v1.2` is the better canonical React learning repository because it contains the later expenses form/filter iteration.

`node_modules` was removed from the tracked project files. Dependencies should be restored with `npm install` inside the relevant app folder.
